import React from 'react';
import styles from './index.scss';

const Title = () => (
  <div
    className={styles.title_block}
    data-testid="title_block"
  >
    <span>Comicomic</span>
  </div>
);

export default Title;
